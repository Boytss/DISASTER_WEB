const corsAnywhere = require('cors-anywhere');

// Create a new CORS Anywhere server
const proxy = corsAnywhere.createServer();

// Listen on a specific port (e.g., 8080)
proxy.listen(5259, () => {
  console.log('CORS Anywhere proxy server is running on http://192.168.1.10:5259');
});